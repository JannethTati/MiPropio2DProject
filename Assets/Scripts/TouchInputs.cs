using UnityEngine;

public class TouchInputs : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Input.touchCount: Cantidad de toques en la pantalla
        //Input.GetTouch(0): Nos da el toque principal
        //TouchPhase.Began: Down
        //TouchPhase.Ended: Up
        //TouchPhase.Canceled: 
        //TouchPhase.Moved:
        //TouchPhase.Stationary:
    }
}
